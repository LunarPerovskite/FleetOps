fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
alembic==1.13.1
pydantic==2.7.0
pydantic-settings==2.2.0
pyjwt==2.8.0
# ═══════════════════════════════════════
# SECURITY PACKAGES
# ═══════════════════════════════════════

# Core cryptography (Fernet, AES, RSA, X509)
cryptography==42.0.0

# Password hashing (bcrypt, argon2, scrypt)
passlib[bcrypt,argon2]==1.7.4
argon2-cffi==23.1.0

# JWT handling (JWS, JWE, JWK support)
pyjwt==2.8.0
python-jose[cryptography]==3.3.0

# OAuth2 / OIDC
authlib==1.3.0

# SSL/TLS certificate handling
pyopenssl==24.1.0
certifi==2024.2.2

# Rate limiting (Redis-backed for production)
slowapi==0.1.9

# Security scanning
bandit==1.7.8
safety==3.2.0

# ═══════════════════════════════════════
# CORE PACKAGES
# ═══════════════════════════════════════

python-multipart==0.0.9
httpx==0.27.0
httpx-auth==0.22.0
redis==5.0.3
celery==5.4.0
pytest==8.2.0
pytest-asyncio==0.23.5
python-dotenv==1.0.1
psycopg2-binary==2.9.9
asyncpg==0.29.0
structlog==24.1.0
